import styles from '../App.module.css'

export default function RightPanel() {
    <div >
        <h3>Add your favourite movies to see here</h3>
    </div>
}

// className="relative right-0 grid-template-columns: repeat(4, minmax(0, 1fr));"